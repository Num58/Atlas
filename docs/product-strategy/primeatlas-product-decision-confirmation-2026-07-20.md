# PrimeAtlas 产品决策确认单

> 版本：v1.0 Approved  
> 日期：2026-07-20  
> 决策人：产品负责人  
> 阶段：Phase 1 产品评审关闭  
> 后续门禁：允许进入 Spec；Spec 未冻结并通过审查前禁止架构实现、任务拆分和开发

---

## 1. 决策结论

PrimeAtlas 产品理解、全量功能 PRD、蓝图—原型—PRD 需求追踪矩阵和技术/QA 门禁已完成一致性审查。产品负责人已确认全部 12 项 P0 产品决策，因此：

- **产品 PRD：Accepted**；
- **进入 Spec：允许**；
- **信息架构：Candidate，未冻结**；
- **架构实现与开发：禁止，直到 Spec Frozen 且门禁通过**。

---

## 2. 已确认的 12 项 P0 产品决策

| ID | 产品决策 | 状态 |
|---|---|---|
| P0-01 | “身份迁移”是 PrimeAtlas 唯一最高层主轴 | Accepted |
| P0-02 | 早期采用“体能单域深扎 + 融合可见”，体能是首发锚点但不是产品终局身份 | Accepted |
| P0-03 | 最多同时主动编排 3 个活跃成长域，每域可有多个目标；超限只建议聚焦，不静默删除 | Accepted |
| P0-04 | v6 六 Tab 仅为信息架构候选，不冻结为最终导航 | Accepted |
| P0-05 | 安全否决后不能继续原危险方案，且不能被用户、调性、编排器或其他 Agent 覆盖 | Accepted |
| P0-06 | 首发仅支持 18 岁以上普通健康成年人；训练方案前须由用户主动、默认未勾选确认不属于未成年人、孕期、急性疼痛、明确疾病、术后康复；命中、拒绝或无法确认时不生成普通精细训练方案，转保守提示/专业转介；确认不替代持续安全审查 | Accepted |
| P0-07 | 小幅、低风险、可逆调整可先执行后通知且允许回退；重大、越界、安全相关调整须事前确认或进入安全通道；画像变化只生成候选 | Accepted |
| P0-08 | 融合入口和证据默认可发现；数据采集和内容生成可分别关闭或撤回，且无惩罚 | Accepted |
| P0-09 | Prime Pulse 可绕过、不是训练硬门槛；拆分长期目标势能、今日准备度、启动仪式；长按产品建议 500ms，提供按钮/辅助技术等效入口 | Accepted |
| P0-10 | 指标默认展示阶段、趋势、证据和数据充分度；只有条件充分时才显示精确百分比 | Accepted |
| P0-11 | 第三方模型仅获取当前任务所需的最小必要、默认去标识化数据，需独立 Consent，用户数据默认不用于训练通用模型 | Accepted |
| P0-12 | 12 Agent 是 12 类产品职责、权限和审查角色，不等于 12 个独立模型、聊天机器人或真人专家 | Accepted |

---

## 3. 信息架构候选

设计阶段已形成三套候选：

| 候选 | 一级导航 | 当前定位 |
|---|---|---|
| A 生命周期四 Tab | 今日 / 旅程 / 成长域 / 我的 | 推荐进入 Spec 与低保真验证 |
| B 身份主轴四 Tab | 今日 / 身份 / 计划 / 我的 | 身份显著度对照组 |
| C v6 收敛五 Tab | 今日 / 身体 / 训练 / 成长 / 我的 | 低迁移成本对照组 |

当前只推荐方案 A 进入 Spec 验证，**不视为最终路由合同**。冻结前必须完成单域/双域/三域、多目标、二级 IA、安全、权限、离线、返回路径和可用性验证。

---

## 4. Spec 必须归零的门禁

在 Spec 中，以下内容必须形成唯一、可测试、可追溯的合同：

1. 产品范围、版本边界和 Won't Have；
2. 页面、路由、一级/二级 IA 和全部状态；
3. 身份、目标、计划、执行、成果、Consent、安全、同步等状态机；
4. API、领域事件、请求/响应、错误码和幂等键；
5. 数据对象、SQLite Schema、索引、生命周期、版本和删除语义；
6. 训练适用范围确认、持续安全审查、替代和复评；
7. 第三方模型字段级最小可见范围、接收方、目的、保留和删除规则；
8. 离线能力矩阵、同步合并、追加事实、冲突解决和时区语义；
9. 指标公式、样本、窗口、误差/置信表达、金标和禁止显示条件；
10. Agent 权限矩阵、SLO、超时、失败接管和 fail-closed；
11. 外部依赖的权限、数据质量、服务可用性和内容有效性四类降级；
12. Design Token、无障碍、性能和 Given/When/Then 验收。

任一相关项未冻结，该 Requirement 保持 `Spec Blocked`，不得进入开发。

---

## 5. 决策日志

- [2026-07-20] Phase 1 - 用户确认全部 12 项 P0 产品决策 - 产品方向和边界已获得正式裁决 - 影响：产品 PRD 升级为 Accepted，允许进入 Spec。
- [2026-07-20] Phase 1 - v6 六 Tab 保持 Candidate - 原型是体能纵向体验样板而非终局 IA - 影响：方案 A/B/C 在 Spec 与低保真验证后再冻结。
- [2026-07-20] Phase 1 - 增加训练适用范围主动确认 - 首发人群和安全边界需显式、可审计 - 影响：命中、拒绝或无法确认均阻止普通精细训练方案，一次确认不替代持续安全审查。
- [2026-07-20] Phase 1 - QA 最终独立验收通过 - 12 项 P0 映射无错位，10 张 Markdown 表格结构完整，高风险 Given/When/Then 覆盖完整 - 影响：Phase 1 关闭，进入 Spec。

---

## 6. 生效范围

本确认单是 Phase 1 产品决策索引。具体产品规则以以下已接受文档为准：

- `primeatlas-product-understanding-review-2026-07-20.md`
- `primeatlas-full-prd-review-2026-07-17.md`
- `primeatlas-blueprint-prototype-prd-traceability-2026-07-17.md`
- `primeatlas-prd-gate-review-2026-07-17.md`
- `primeatlas-information-architecture-options-2026-07-20.md`

下一阶段由 `PrimeAtlas Spec v1.0` 将上述产品规则转换为唯一工程合同。